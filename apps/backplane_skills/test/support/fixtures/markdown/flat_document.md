This is a flat document with no headings.

It contains multiple paragraphs of text but no markdown heading markers.

The parser should handle this gracefully, treating the entire content
as a single chunk or applying its default chunking strategy.

Some code inline: `mix test` and `iex -S mix`.
