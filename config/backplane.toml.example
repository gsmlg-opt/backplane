# Backplane Configuration (boot-only)
# Operational config (upstreams, providers, credentials, skills) is managed
# through the admin UI endpoint and stored in PostgreSQL.

[backplane]
host = "0.0.0.0"
port = 4100

[database]
url = "postgres://localhost/backplane_dev"
