#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

mapfile -t tests < <(find apps -path '*e2e_test.exs' -print | sort)

if [ "${#tests[@]}" -eq 0 ]; then
  echo "No e2e tests found under apps/" >&2
  exit 1
fi

devenv shell -- mix test "${tests[@]}" "$@"
