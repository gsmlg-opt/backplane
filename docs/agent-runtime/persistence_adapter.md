# Agent runtime persistence adapter

The package ships the current [persistence contract and Sigma adapter guidance](../../apps/backplane_agent_runtime/PERSISTENCE.md), including `Store.fence/6`, reusable `StoreConformance.run/3`, and the distinction between ephemeral execution, disk-backed test evidence, and production durability.
