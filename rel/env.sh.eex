#!/bin/sh

case "$RELEASE_COMMAND" in
  migrate)
    if [ -n "$2" ]; then
      echo "ERROR: migrate does not accept arguments" >&2
      exit 1
    fi

    exec "$RELEASE_ROOT/bin/$RELEASE_NAME" eval 'Backplane.Release.migrate()'
    ;;
esac
